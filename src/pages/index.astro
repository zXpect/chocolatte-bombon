---
import Layout from '../layouts/Layout.astro';
import SocialLink from '../components/SocialLink.astro';
import ProductCard from '../components/ProductCard.astro';
---

<Layout title="Chocolatte Bombon | Pastelería Artesanal">
	<main>
		<section class="hero">
			<div class="hero-content">
				<h1>Chocolatte Bombon</h1>
				<p class="tagline">Dulces momentos, recuerdos eternos</p>
				<a href="#productos" class="cta-button">Descubre nuestros productos</a>
			</div>
		</section>

		<section id="productos" class="products">
			<div class="section-header">
				<h2>Nuestras Creaciones</h2>
				<div class="decorative-line"></div>
			</div>
			<div class="product-grid">
				<ProductCard
					title="Pasteles Personalizados"
					image="/pastel-personalizado.jpg"
					description="Diseños únicos para tus momentos especiales"
				/>
				<ProductCard
					title="Cupcakes Artesanales"
					image="/cupcakes.jpg"
					description="Pequeños placeres llenos de sabor"
				/>
				<ProductCard
					title="Postres Tradicionales"
					image="/postres.jpg"
					description="El sabor de siempre con un toque moderno"
				/>
			</div>
		</section>

		<section id="historia" class="about">
			<div class="about-content">
				<div class="section-header">
					<h2>Nuestra Historia</h2>
					<div class="decorative-line"></div>
				</div>
				<p>Con más de 50 años de experiencia, Chocolate Bombon ha endulzado los momentos más especiales de nuestros clientes con creaciones únicas y sabores inolvidables.</p>
			</div>
		</section>

		<section class="social">
			<div class="section-header">
				<h2>Síguenos</h2>
				<div class="decorative-line"></div>
			</div>
			<div class="social-grid">
				<SocialLink
					platform="facebook"
					username="chocolatebombon"
					icon="facebook"
				/>
				<SocialLink
					platform="instagram"
					username="chocolatebombon"
					icon="instagram"
				/>
				<SocialLink
					platform="tiktok"
					username="chocolatebombon"
					icon="tiktok"
				/>
			</div>
		</section>

		<section id="contacto" class="contact">
			<div class="contact-container">
				<div class="section-header">
					<h2>Contáctanos</h2>
					<div class="decorative-line"></div>
				</div>
				<div class="contact-info">
					<div class="contact-card">
						<span class="icon">📞</span>
						<h3>Teléfono</h3>
						<p>(123) 456-7890</p>
					</div>
					<div class="contact-card">
						<span class="icon">📍</span>
						<h3>Dirección</h3>
						<p>Calle Principal #123<br>Ciudad, Estado</p>
					</div>
					<div class="contact-card">
						<span class="icon">✉️</span>
						<h3>Email</h3>
						<p>info@chocolatebombon.com</p>
					</div>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	main {
		width: 100%;
		color: var(--color-text);
		padding-top: 60px;
	}

	.hero {
		height: 90vh;
		background-image: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)),
			url('/hero-background.jpg');
		background-size: cover;
		background-position: center;
		background-attachment: fixed;
		display: flex;
		align-items: center;
		justify-content: center;
		text-align: center;
		color: white;
	}

	.hero-content {
		padding: 3rem;
		background-color: rgba(0, 0, 0, 0.6);
		border-radius: 1rem;
		backdrop-filter: blur(5px);
	}

	.cta-button {
		display: inline-block;
		margin-top: 2rem;
		padding: 1rem 2rem;
		background-color: var(--color-secondary);
		color: white;
		text-decoration: none;
		border-radius: 50px;
		font-weight: 500;
		transition: transform 0.3s ease, background-color 0.3s ease;
	}

	.cta-button:hover {
		transform: translateY(-2px);
		background-color: var(--color-primary);
	}

	h1 {
		font-size: 5rem;
		font-weight: 700;
		margin-bottom: 1rem;
		color: white;
	}

	.tagline {
		font-size: 1.8rem;
		font-weight: 300;
		font-family: var(--font-primary);
	}

	section {
		padding: 6rem 2rem;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-header h2 {
		font-size: 3rem;
		margin-bottom: 1rem;
	}

	.decorative-line {
		width: 100px;
		height: 3px;
		background-color: var(--color-secondary);
		margin: 0 auto;
	}

	.product-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 3rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	.about {
		background-color: white;
		position: relative;
		overflow: hidden;
	}

	.about-content {
		max-width: 800px;
		margin: 0 auto;
		text-align: center;
	}

	.about p {
		font-size: 1.3rem;
		line-height: 1.8;
		color: var(--color-text);
	}

	.social {
		background-color: var(--color-background);
	}

	.social-grid {
		display: flex;
		justify-content: center;
		gap: 3rem;
		margin-top: 2rem;
	}

	.contact {
		background-color: white;
	}

	.contact-container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.contact-info {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-top: 3rem;
	}

	.contact-card {
		background: var(--color-background);
		padding: 2rem;
		border-radius: 1rem;
		text-align: center;
		transition: transform 0.3s ease;
	}

	.contact-card:hover {
		transform: translateY(-5px);
	}

	.contact-card .icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
		display: block;
	}

	.contact-card h3 {
		margin-bottom: 1rem;
		color: var(--color-primary);
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 3rem;
		}

		.tagline {
			font-size: 1.4rem;
		}

		section {
			padding: 4rem 1rem;
		}

		.social-grid {
			flex-direction: column;
			align-items: center;
		}
	}
</style>